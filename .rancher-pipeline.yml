stages:
- name: BuildImage
  steps:
  - publishImageConfig:
      dockerfilePath: ./dockerfile/Dockerfile
      buildContext: .
      tag: jkritzen/jumphost
      registry: index.docker.io
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./jump.yaml
timeout: 60
notification: {}
